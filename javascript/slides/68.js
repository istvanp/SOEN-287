// @title Arrays: join()
// @desc <code>string = arrayName.join(separator)</code> joins all elements of an array into a string.

var arr = [1, 5, 3, 7];
str = arr.join(' + ');
str += " = 16";
document.writeln(str);